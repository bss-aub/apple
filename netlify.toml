# Nothing is built — this site is only the signing function.
[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  # "nft" copies the packages as they are instead of rewriting them, which is
  # the safest choice for node-forge.
  node_bundler = "nft"
  node_version = "22"
